name: Deploy

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: ./.github/workflows/steps/checkout_code.yml

      - name: SSH and Deploy
        uses: ./.github/workflows/steps/ssh_and_deploy.yml
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Send Notification
        uses: ./.github/workflows/steps/send_notification.yml
        with:
          server_address: mail.brahmware.com
          server_port: 465
          username: ${{ secrets.MAIL_SERVER_UID }}
          password: ${{ secrets.MAIL_SERVER_PASS }}
          to: "bhunia.sanchayan@gmail.com"
          from: ${{ secrets.MAIL_SERVER_UID }}
